<Window x:Class="Elmish.WPF.Samples.SubModelSeq.List.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elmish.WPF.Samples.SubModelSeq.List"
        xmlns:vm="clr-namespace:Elmish.WPF.Samples.SubModelSeq.List;assembly=SubModelSeq.List"
        Title="SubModelSeq"
        Height="800"
        Width="1100"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        d:DataContext="{x:Static vm:Program.mainDesignVm}">
  <Grid Margin="0,20,0,10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Button Grid.Row="0" Command="{Binding AddCounter}" Content="Add counter" Width="150" Margin="10,0,10,20" />
    <ListBox Grid.Row="1" ItemsSource="{Binding Counters}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding CounterIdText}" Width="250" Margin="10,5,10,5" />
            <local:Counter/>
            <Button Command="{Binding Remove}" Content="×" Margin="0,5,10,5" Width="20" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</Window>
