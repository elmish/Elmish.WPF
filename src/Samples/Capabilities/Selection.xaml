<UserControl x:Class="Elmish.WPF.Samples.Capabilities.Selection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <StackPanel Orientation="Horizontal">
    <StackPanel Orientation="Vertical">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="Selected index: "/>
        <TextBlock Text="{Binding SelectedIndex}"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" Margin="6">
        <Button Command="{Binding DeselectIndex}" Content="Deselect" Height="25" Width="75" Margin="6"/>
        <ListBox ItemsSource="{Binding SelectedIndexData}" SelectedIndex="{Binding SelectedIndex}" Margin="6"/>
      </StackPanel>
    </StackPanel>
    <TextBlock Text="The simplest way to handle selection is via an optional TwoWay binding on SelectedIndex." TextWrapping="Wrap"/>
  </StackPanel>
</UserControl>