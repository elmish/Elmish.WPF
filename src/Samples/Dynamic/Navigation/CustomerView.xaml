<UserControl x:Class="Navigation.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
  <DockPanel LastChildFill="True">
    <TextBlock Text="{Binding Title}" DockPanel.Dock="Top" FontSize="16" Margin="10" />
    <StackPanel Orientation="Vertical" Margin="10">
      <Button Content="Get Current Time" Command="{Binding GetTime}" Margin="5" />
      <TextBlock Text="{Binding CurrentTime, StringFormat='yyyy-MM-dd HH:mm:ss.fffff zzzzz'}" Margin="5" />
    </StackPanel>
  </DockPanel>
</UserControl>